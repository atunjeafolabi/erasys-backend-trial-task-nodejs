{
    "initialRuleset": {
        ".{5}": "Password length must be minimum 5 characters",
        "\\d": "At least one digit must be used in the password",
        "^(?:(.)(?!\\1{2}))+$": "No more than two repeating characters allowed (like 'bbb' – 2 are allowed, but not 3 or more)",
        "^(?=.*[A-Z])|(?=.*[!@#$%^&*])": "Password must contain at least one upper-case character OR alternatively one special character"
    }
}